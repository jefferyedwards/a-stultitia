# Kafka transport configuration (active when spring.profiles.active=kafka)
kafka.role=producer
kafka.topic-name=time-of-day

# Bootstrap servers is passed via command line by run-producer-kafka.sh
# (reads the address from the file written by the consumer's embedded broker)
