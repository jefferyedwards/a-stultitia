# Kafka transport configuration (active when spring.profiles.active=kafka)
kafka.role=consumer
kafka.topic-name=time-of-day
kafka.group-id=timeofday-consumer

# Start embedded Kafka broker in the consumer process
kafka.broker.embedded=true
